<!--
City Grid -> location/:id
-->

<% @locations.each do |location| %>
    <%= link_to(location_path(location)) do %>
      <img src="<%= location.photo_url %>" alt="" width="150">
      <%= location.name %>
    <% end %>
<% end %>

